/target

# The working copy at the repo root is a build output; the released binary
# lives in web/download/ and IS committed, because fzip.org serves it directly.
# See WEBSITE.md for why the download is not on GitHub Releases.
/fzip.exe
/fzip-norar.exe
